# Tempo — trace storage (single-binary, local filesystem for dev).

tempo:
  # 48h retention (chart default is 24h)
  retention: 48h
  # Disable anonymous usage reporting to Grafana Labs.
  reportingEnabled: false
